<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.slfinalproject.commurest.board.repository.BoardMapper">
    <!-- 게시글 추천수 -->
    <update id="updateLike" parameterType="int">
        update board set
        recommend = recommend+1
        where bno = #{bno}
    </update>

    <!-- 게시글 추천수취소 -->
    <update id="updateLikeCancel" parameterType="int">
        update board set
        recommend = recommend - 1
        where bno = #{bno}
    </update>

    <!-- 게시글 추천 시 Like 테이블에 insert -->
    <insert id="insertLike">
        insert into recommend(recommend_no , bno , board_id)
        values((SELECT NVL(MAX(recommend_no), 0) + 1 FROM recommend) ,#{bno} ,#{boardId})
    </insert>

    <!-- 게시글 추천취소 시 delete -->
    <delete id="deleteLike">
        delete from recoomend where bno = #{bno} and user_id = #{user_id}
    </delete>

    <!-- 게시글 추천 시 Check를 1로 만들어서 중복방지-->
    <update id="updateLikeCheck">
        update recommend set recommend_check = 1
        where bno = #{bno} and user_id = #{user_id}
    </update>

    <!-- 게시글 추천취소 시 다시 0  -->
    <update id="updateRecommendCheckCancel">
        update recommend set recommend_check = 0
        where bno = #{bno} and user_id = #{user_id}
    </update>

    <!-- 게시글 추천 중복방지 select문 -->
    <select id="recommendCheck" resultType="int">
        select count(*) from recommend where bno = #{bno} and user_id = #{user_id}
    </select>

</mapper>